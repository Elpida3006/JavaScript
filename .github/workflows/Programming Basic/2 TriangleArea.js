function triangleArea(input) {
    let a = Number(input.shift());
    let h = Number(input.shift());
    let area = (a * h / 2).toFixed(2);
    
    console.log(area);

}
triangleArea();

function triangleArea(input) {
    let a = 20;
    let h = 30;
    let area = (a * h / 2).toFixed(2);
    
    console.log(area);

}
triangleArea();

function triangleArea() {
    let a = 15 , h = 35 , area = a * h / 2;
    console.log(area.toFixed(2));
}
triangleArea();


function triangleArea() {
    let a = 7.75 , h = 8.45;
    let area = a * h / 2;
    console.log(area.toFixed(2));
}

triangleArea();

function triangleArea() {
    let a = 7.75 , h = 8.45;
    let area = (a * h / 2).toFixed(2);
    console.log(area);
}

triangleArea();

function triangleArea(input) {
    let a = 1.23456
    let h = 4.56789
    let area = a * h / 2;

    console.log(area.toFixed(2));
    
}