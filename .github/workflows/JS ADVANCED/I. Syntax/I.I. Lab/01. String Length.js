function stringLength(firstArg, secArg, thirthArg) {

    let length1 = firstArg.length
    let length2 = secArg.length

    let length3 = thirthArg.length




    console.log(length1 + length2 + length3);
    console.log(Math.floor((length1 + length2 + length3) / 3));



}
stringLength('chocolate', 'ice cream', 'cake')